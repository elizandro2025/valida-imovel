name = "validaimovel-payment-api"
main = "src/index.ts"
compatibility_date = "2026-07-28"
compatibility_flags = ["nodejs_compat"]

workers_dev = true

routes = [
  { pattern = "api.validaimovel.com", custom_domain = true }
]

[vars]
ENVIRONMENT = "production"
